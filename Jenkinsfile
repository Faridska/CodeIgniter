pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build project CodeIgniter'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing project'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying project'
            }
        }
    }
}
